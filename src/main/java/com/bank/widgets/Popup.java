package com.bank.widgets;

import javafx.scene.Scene;
import javafx.stage.Stage;

public class Popup extends Stage {
    public Popup(String msg) {
        Stage popupStage = new Stage();

        
        Scene scene = new Scene(null);
    }
}
